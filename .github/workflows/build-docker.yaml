name: build-docker
on: pull_request
jobs:
  update-docker-image:
    runs-on: [ubuntu-latest]
    env:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
    steps:
      - uses: actions/checkout@v2
      - name: build-image
        run: bash bin/update-docker.sh

